<h1>Hi peppernoter!</h1>
<p>Change of your password in Peppernote(<%= url_for(:action => 'home', :controller => 'pages', :only_path => false) %>) was requested. You can change it through the link below</p>

<p><%= link_to 'Change password', edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

<p>If you didn't request this, please ignore this email.</p>
<p>Your password won't change until you access the link above and create a new one.</p>
